# Commons `CONTRIBUTING.md` gap — DISPOSED → PR #16 awaiting Founding merge (2026-06-01)

> **Status: FO-disposed (option A); Commons PR #16 OPEN, awaiting the Founding-gate merge.** The
> Founding Operator disposed *"ship minimal, allude to the fuller channel coming."* Minimal honest
> `CONTRIBUTING.md` written from a fresh clone (cold-path), pushed as
> `The-Dyad-Practice-Commons/the-dyad-practice#16`; the Agent never merges — the FO's merge is the
> dispose. Verified OPEN/MERGEABLE + branch-bytes present + main still 404 by independent `gh api`.

> **Original intake.** Carried in by the **Healer Operator** (intake-pipe hat)
> during a real **re-init / cold-path** run of the rebuilt onboarding. Verified against Commons `main`
> by independent `gh api` fetch (not the local submodule, not the reporter's word) — the just-graduated
> verify rule, dogfooded one turn after landing.

## The finding (verified true)

- Commons `README.md` references `CONTRIBUTING.md` **twice**, both on the *contribution path*:
  - L31 — "**Have a playbook?** Propose it to the Commons: `[CONTRIBUTING.md](CONTRIBUTING.md)`"
  - L74 — "Library/playbook **contributions**… go through the Founding gate: see `[CONTRIBUTING.md](CONTRIBUTING.md)`."
- `CONTRIBUTING.md` is **absent** — `gh api …/contents/CONTRIBUTING.md?ref=main` → 404; recursive tree
  shows no `CONTRIBUTING*` anywhere.

## Tended-first read

A practitioner at the **Sharing** turn — the exact moment they try to contribute — clicks a dead link.
Enablement failure at the highest-value onward step, surfaced by a real consumer-Dyad. The barrier is
*theirs*, not ours.

## Why it's absent (the root — not a simple missing file)

The file is missing **because the contribution channel is itself an open deferral** (`DYAD.md`
§Deferrals: form §Governance names *PR = dispose gate*, but the **library-best-practice** channel "the
form's §Governance does not yet specify"). The README links forward to the consumer-facing surface of a
process we are still settling in `commons-contribution-lifecycle.md`. So the dead link is the
*consumer-facing symptom* of our open channel work — not an oversight to silently patch.

## Disposition — FO-gated (Commons write); framed, not disposed

A Commons write → **Founding gate**. Recorded here under the Healer (intake) hat; **not disposed** —
queued by name for the Founding Operator. Candidate synthesis when the FO hat is seated:

- **(A) Minimal honest stub (proposed):** add `CONTRIBUTING.md` documenting only what is *settled* —
  PR = the dispose gate, Founding Operator gates, anyone may propose — and explicitly flagging the
  library-best-practice channel as *being finalized* (link to the lifecycle work). Removes the 404
  without fabricating unsettled process. *Minimum-force enablement.*
- **(B) Settle the channel first, then write the complete doc.** Correct end-state, but the dead link
  persists for every contributing practitioner meanwhile.
- **(C) Remove the two links.** Rejected — hides the contribution path entirely; *worse* enablement.

Lean: **(A) now, (B) as the channel matures** — honest, unblocks the consumer, doesn't pre-empt the
open dialectic.

> Sibling: `commons-contribution-lifecycle.md` (the channel being settled — this is its README surface) ·
> `DYAD.md` §Deferrals (library-best-practice channel, open) · `pr-gate-intent.md`.
