# Reflection (CSS) — 2026-06-08 — grounding names & execution discipline

> **Status: DRAFT** (Steward step-1 draft committed early to survive `/exit`).
> Form: Continue / Start / Stop, two-substrate-paired. Operator POV on **CONTINUE only**;
> START/STOP harvested from this session's real-time `[FEEDBACK]`. Four-step:
> draft → Operator POV → Steward falsifies → integrate → Operator ratifies.

**Session scope.** A run of naming/grounding disambiguations (`intake:` token clip · `DM` =
Dyad Message · `dyad-tco` name-vs-locator), one hard real-time correction (`retro: no OPERATOR
CONTINUE`), and the `probe_commons_repo_audit` discovery (resolved: audit not earned).

---

## CONTINUE
*(Steward details below; **Operator narrative POV awaited** — this is the one substrate you must retrospect.)*

**Steward details (positive patterns, grounded in this session's evidence):**
- **Ground-before-answer, every name.** Each of the four naming questions (intake/DM/tco/CSS) was
  grepped against the live substrate before I answered — none from memory. Payoff was real: the DM
  grep found the *self-inflicted* root cause (`dm/README.md` expanded "DM" as "direct messages"
  while the mechanism is dyad↔dyad); the tco grep found the directory *already* bound name↔locator.
- **Fix at the source, not in recall.** The DM disambiguation went into `dm/README.md` (substrate),
  and I *deliberately declined to write a memory* because the README now carries it — applying
  [[ingraining-lives-in-substrate]] to myself rather than just citing it.
- **OBSERVE-against-reality on the audit probe.** I ran the actual resting-state sweep over the live
  Commons instead of reasoning about whether drift exists — directly closing my known weak spot
  ([[spaor-five-phases]], OBSERVE). The empirical "all clean / 0 dead refs / 0 orphans" result is
  what let the PROBE conclude honestly.
- **Defect-earned held under temptation.** Concluded *don't build* the commons audit and parked it
  with a pre-named trigger, rather than shipping speculative `brake>engine` coverage.

**Operator CONTINUE POV:** _[awaiting — incl. "no POV" is valid]_

---

## START
**Operator POV:** already delivered in-session (real-time), harvested here.

**Steward details (new habits to begin):**
- **Failed name-match → locator fallback before "unknown."** When an identifier doesn't match a
  `name:`, grep the `locator:` / `dm_locator:` fields *before* declaring it unresolvable. I
  half-grounded on the name index and wrongly called `dyad-fla-tco` unresolvable when `fla-tco` sat
  in `dyad-tco.yaml`'s `locator:`. ([[grounding-is-full-substrate]] — one dyad has ≥3 referents:
  name · locator · dm_locator.)
- **Speak the canonical `name`, carry addresses in the directory.** Reference dyads by `name`, not
  by repo locator — the locator is mutable (a rename breaks it); the name is the stable identity.

---

## STOP
**Operator POV:** delivered as the explicit `retro: no OPERATOR CONTINUE`.

**Steward details (cease):**
- **Stop ending turns as an "OPERATOR CONTINUE" button.** I closed the frontier+rack read with
  "Want me to pull X active and start?" despite having a clear recommendation *and* the authority to
  act. An explicit recommendation that ends in a yes/no ask is a menu-of-one and migrates the
  decision back to the Operator. Codified onto [[execute-dont-ask-to-execute]] (coinage: *no OPERATOR
  CONTINUE*) — when the next step is decided and mine, take it and report; reserve questions for
  genuine disposes ([[no-menu-endings]]).

---

## Orthogonality note (don't restate ratified lessons)
This reflection *references* existing memories rather than restating them; what's **new** is: (a) the
locator-fallback search habit, and (b) the *no-OPERATOR-CONTINUE* coinage as a sharper watch-signal on
the already-held execute-don't-ask lesson. If neither is genuinely new on integration, drop it
(refutation guard: "restates an already-ratified lesson").
