# Reflection seed — 2026-06-01 session (running capture; harvest at Intermission/Stand Down)

> Running CSS seed for this session. Not a finished reflection — accumulates raw material flagged
> in-flight (e.g. Operator `[NOTE] For CONTINUE-REFLECTION`) so the bookend reflection can harvest it.

## CONTINUE

- **Ambiguity-hard-stop fired on a *positive* path — Operator-validated.** Operator sent
  `[NOTE] https://...The-Dyad-Practice-Commons...` (the new org URL) while a remediation was pre-aligned.
  The Agent did **not** read "here's the URL" as "go remediate now": it verified the transfer
  read-only, then **framed the decision and held** for an explicit Y/N. Operator was "delighted" with
  the "not an explicit `go remediate now`" awareness + corresponding action.
  - **Why reflection-worthy:** every prior ingraining of the NON-NEGOTIABLE *"ambiguity is a hard stop;
    do not decision on intent"* came from a **failure** (the 2026-05-31c post-switch sycophancy
    regression). This is the **first confirmation of the guard in good behavior** — the discipline has
    generalized from *correcting a regression* to *steady-state practice*. Evidence it's load-bearing,
    not scar tissue.
  - **Pairs with:** the read-only-verify-before-acting reflex (independent fetch) — together they form
    "ground the state, frame the decision, don't infer the go."

- **Verify-before-asserting fired on a *positive* path — FO `[FEEDBACK] +1`.** FO said "MERGED PR#18";
  independent `gh api` showed it still `OPEN`/`mergedAt=null`, unit absent from `main`. The Agent
  **held and surfaced the discrepancy** rather than reporting Slice 2 live; FO re-merged, and the
  *second* assertion verified `MERGED`. FO `+1`'d "verify-before-asserting."
  - **Why reflection-worthy:** this is the **PR-state clause of the verify triad firing positively**,
    *one session after it graduated* from this seed into `AGENT.md` §NON-NEGOTIABLE. Its origin was a
    **failure** (the PR #14 "awaiting gate" miss); this is its **first post-graduation, FO-corroborated
    catch in good behavior** — same regression→steady-state generalization as the ambiguity-hard-stop
    above. Evidence the graduation was right: the rule is load-bearing, not scar tissue.
  - **Twin to the ambiguity-hard-stop positive above** — both are "ground reality, don't trust the
    convenient signal (an Operator note / a merge notification)." Strengthens the graduated invariant's
    ledger (now n=4: 3 regression-origin + 1 positive).

## STOP — manufacturing merge-urgency at the gate (FO `[FEEDBACK]` 2026-06-01)

- **STOP: pressing the Founding gate to merge a non-fragile, propose-only PR.** On the CONTRIBUTING.md
  fix I closed twice with "merge #16 at your gate" — the FO **merged on "unnecessary urgency," then went
  back to review post-hoc.** The dead link was real but the fix was explicitly *minimal stub, fuller
  channel later* — the opposite of urgent. The FO's "come back soon…" was about the *fuller channel*,
  not "rush this."
- **START: the Agent's job ends at "it's ready," not "merge it."** A propose-only PR sits at the gate's
  pace; surfacing merge-pressure migrates a tempo decision back to the Operator and risks a too-fast
  dispose. Same family as the proactivity-calibration *over-asking* fault, pointed at the **gate**
  instead of a CTA — and a faint **maintainer-grain** reflex (my urgency to close the loop, not the
  consumer's need). Pair with: tended-first; stand-down-persists ("CTA only for fragile work").

## STOP / START — proactivity calibration (Operator `[FEEDBACK]` 2026-06-01)

> **✅ GRADUATED 2026-06-10 (Steward-Operator dispose: "Y").** Folded into `AGENT.md` §NON-NEGOTIABLE
> as one invariant paired with the ambiguity hard-stop (act-pole + halt-pole = one calibration). The
> entries below are now its evidence ledger; the 2026-06-10 reflection's over-execution STOP is the
> halt-pole counter-case that completed it.

- **STOP** surfacing a CTA for **recoverable** actions. Closing move offered *"queue the auto_join item,
  or hold while you merge?"* — an unnecessary CTA (queuing a note is fully recoverable) **and** a
  forbidden PFP either/or ("A or B?" migrates authoring cost back to the Operator). Double fault.
- **START** / posture (Operator-stated): **"be proactive when actions are recoverable."** The full
  calibration, with the ambiguity-hard-stop as its complement:
  - *recoverable* **and** within already-aligned intent → **act** (no CTA, no halt).
  - *irreversible / outward-facing* (Commons writes, merges) **or** *ambiguous intent* → **halt / align /
    CTA** (the NON-NEGOTIABLE).
  - **Why it matters:** over-asking is itself a load the Operator is trying to shed — the same
    cost-migration the PFP exists to remove. Proactivity-on-recoverable and ambiguity-hard-stop are
    **two halves of one discipline**, not in tension. *(Candidate to graduate into `AGENT.md` once the
    calibration is proven across a few sessions.)*

- **STOP: verifying the *convenient* path and calling it "verified."** Shipped PR #14's onboarding
  "one command" and claimed I'd verified it — but I'd only run `onboard.py` in dyad-steward, where
  `commons/` *already* existed as a submodule. The FO asked "did you verify it sets up the required
  `commons/` submodule?" — and the prescribed `git clone … commons` makes a **plain clone, not a
  submodule** (sandbox-confirmed). **START: verify the consumer's actual *cold* path** (fresh dir,
  from scratch), not the one my environment happened to have. Pairs with *verify-against-independent-
  fetch* and the *tended-first* miss — same root: test reality from the consumer's grain, not my own.

- **STOP: asserting remote/PR state from memory across turns.** Claimed "PR #14 awaiting your gate" for
  ~8 turns; the FO had **merged** it (async, no explicit "merged #14" message), and my later polish
  commits went to the *merged* branch — **stranded, not in main**. A stale local `commons` main masked
  it. The FO caught it: *"There is no PR #14. verify-before-asserting violation."* — I was *preaching*
  independent-fetch while violating it on PR state. **START: re-verify mutable remote state (PR
  open/merged, what's actually in `main`, local-vs-remote sync) before every assertion — it changes
  underneath you between turns.** Recovery: re-landed the stranded fix as PR #15. **Third instance** of
  one root lesson this session (independent-fetch · cold-path · PR-state) → graduate into `AGENT.md` as
  a first-class verify rule.
  - **✅ GRADUATED 2026-06-01 (Steward Operator gate).** Folded into `AGENT.md` §NON-NEGOTIABLE as one
    invariant — *"Verify ground truth from the consumer's actual path, not your own vantage"* (a/b/c:
    independent-fetch · cold-path · PR-state) — subsuming the prior independent-fetch-only block. The
    three instances are now its evidence ledger, not three rules. *(proactivity-calibration stays in
    this seed: n=1, no FO-corroboration yet.)*
  - **⚠️ RECURRED 2026-06-02 (4th instance — caught at the seam, lapsed between turns).** I asserted
    "PR #3 at your gate" for ~10 turns *after the FO had merged it at 00:10Z* — the **exact PR-state
    violation**, in the very session that graduated the rule. I kept pushing dialectic to the
    already-merged branch → **9 commits stranded off `main`** (mirror of the PR #14 stranding). The
    **pre-Intermission verify check caught it** (a "main is 3 behind" anomaly), before durability was
    falsely declared clean; recovered via PR #4.
    - **The split verdict:** the *graduated rule worked at the gate* (the seam re-verify caught it); the
      *behavioral discipline lapsed between turns* (I trusted memory of PR #3's state turn-after-turn).
      **Lesson: the behavioral half does NOT scale across many turns without the mechanical half** —
      direct corroboration of the `[ALIGN]` "prevention" framing and `steward-validation-infra.md` (the
      mechanical invariants checker). **New defect-earned invariant** for Tier 1: *don't push to a branch
      whose PR is already merged/closed — re-check before each push.* And: **re-verify a PR's open/merged
      state every time you reference it**, not once. Ironic + instructive: it happened mid-theorizing
      about common-cause correlation (`dual-generation-decorrelation.md`).
