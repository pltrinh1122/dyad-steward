# d-reflect — 2026-07-16 — dogfooding the DIP-convergence + README disciplines to define steward's craft

Form: CSS (Agent-provenance) + SH (Operator-provenance, descriptive, verbatim-quoted; Should-Have gated by a
materiality bar — the miss produced a claim found false by a mechanical check). Lands by commit; no CTA.

**Arc:** redefined steward's four craft slots (`craft` · `craft_telos` · `craft_value` · `craft_invariant`)
by dogfooding the peer **dip-convergence** discipline (lineage aule→chiron→milo) and **dyad-cairn**'s
README-writing discipline — *before* proposing any DIP form-canon change. Deliverables: `DYAD.md §Craft`,
`README.md` manifesto ("The Training Wheel"), `dialectic/dip-convergence.md` ledger. FR to the FO gate:
deferred (dogfood first).

## CSS — Agent

**Continue**
- **Spine-before-wording (the lineage's "one rule").** Held every slot: elicit the spine + confirm
  scope/altitude, propose wording only after. It caught a real re-level at nearly every slot (telos gauge,
  craft breadth, value shape). The whole arc's shape was spine-first.
- **Verify-from-source on peer claims.** The peer-remote survey ran the actual `gh api` bytes, not memory —
  and falsified *both* proposed slot-names (`craft_object`, `craft_target`) down to bare `craft`, plus the
  ballooned content down to a tight essence. The NON-NEGOTIABLE, working.
- **Per-event honest ledger.** Recorded each re-level and sink as it happened, including reversing a slot I'd
  marked LOCKED — the record didn't smooth over the detours.

**Start**
- **Survey the reference dyads' actual slots at slot 1, not slot ~6.** I coined `craft_object` and built on it
  for three turns (apparatus → "complete discipline-set") before a survey grounded it back to the
  peer-universal bare `craft`. The dip-convergence discipline's own accelerator — *"study the reference dyad's
  process"* — prescribes this up front; I fetched milo/chiron early but didn't survey the full roster's craft
  slots until directed.

**Stop**
- **Proposing forks/framing ahead of the confirmed spine.** Slot 1's "narrow vs wide" altitude fork was
  mis-cut (it conflated `craft` with the *tended*); slot 3's "value = falsification" narrowed the whole craft
  to one pillar. Both are the lineage's chief sink — form ahead of spine — and I repeated it. The counter is
  already in hand: confirm the spine, *then* offer at most one candidate, never a fork over an unpinned frame.
- **Ballooning a slot.** "The complete discipline-set, necessary+sufficient" violated aule's *keep-it-tight*
  discipline; the essence stays tight, the discipline-set is the *how*.

## SH — Operator

**Should-Have** *(materiality bar: found false by a mechanical check)*
- **Should have surveyed peer remotes before coining `craft_object`.** My coinage and its "complete
  discipline-set" content were found false by the mechanical survey (6/6 form-inheriting dyads use bare
  `craft`; the slot holds a tight activity-essence). The Operator had to direct the falsification:
  > *"craft_target may be more accurate than craft_object. falsify with survey of peer dyads' remote repo."*
  Three turns of object/apparatus scaffolding would not have been built had the survey run at slot 1.

  *(No second Should-Have manufactured. The mis-cut forks were corrected by Operator riff, not by a mechanical
  check, so they are CSS-Stop items, not Should-Haves — no manufactured balance.)*

**Should-Hold** *(Operator moves to keep)*
- **Final-lock in totality, because slots co-constrain.**
  > *"will perform final lock in totality because each slot influences each other."*
  Held literally: the README arc re-levelled the already-locked `craft_telos` (terminal → no-dependency;
  self-* → acceptance criteria). Locking each slot tentatively and reserving the hard lock for the whole set
  is what made that re-level cheap instead of a contradiction.
- **Dis-conflate Belief into Claim 0 (the problem) + Claim 1 (the solution/spark).**
  > *"i've learned to dis-conflate Belief into Claim 0 (the problem with the Default...) and Claim 1 (the
  > solution which sparked the creation of this dyad...)."*
  Learned from another dyad; sharpens cairn's C11 (its single `belief` slot → problem/solution pair). A
  candidate contribution back to cairn's discipline.
- **Read the acceptance-criteria structure of a telos.**
  > *"isn't self-forming/healing/governing are acceptance criteria of the telos of no-dependency?"*
  Caught the agent folding the three self-* into the terminal; the cleaner cut is one terminal (no dependency)
  + a 3-part gauge. Held in `DYAD.md §Craft`.

## Harvest (→ ledger, not restated here)

The redefinition's cross-cutting findings — steward as the *forged* (not `NOT_YET_WORN`) `craft_invariant`
specimen; the `object ≈ craft` meta-craft collapse (second instance after `value ≈ invariant`); the C11
dis-conflation as a cairn-contribution candidate; and the "Dim#1 is mislabeled `craft_telos` when its content
is the `craft` essence" form-gap — live in `dialectic/dip-convergence.md` (§Open findings for the eventual FR),
gated behind lived cycles before any FR to the FO.
