# Reflection (D3 · CSS+OR) — the G0-ABC → operate-model arc

*Reflect-form **adopted from dyad-bond** (`kb/reflection-discipline.md`, CSS+OR, kb-with-caveat). This is
its **first cross-dyad application** — a coverage datum on the cross-dyad axis (same-human, so L2b
cross-human stays open per bond's own caveat). Landed by being written + committed; **no CTA** — honoring
the form's own anti-"post-land-CTA-relapse" STOP.*

**Scope:** 2026-06-30 → 07-04 — refereeing bond's G0 package → the Dyad-as-ABC falsification campaign →
the operate-model DIP restructure (form `AGENT.md` PR #75, merged `ce2bf9a`) → the four-dyad peer contest.

## CONTINUE

- **(OR) Falsification-by-instance-grounding.** The Operator drove every structural claim to ground against
  real dyads — *"test against bond and steward instances," "now cairn/touchstone/wu-wei," "include healer."*
  That refuted my clean two-layer model into the grounded three-layer one **and** exposed the confound in
  "Finding 1" (the meta-dyad convergence wasn't independent). *Keep: the Operator's demand for
  instance-grounding is what turns a plausible model into a survived one.*
- **(OR) Dispose/implement sequencing.** *"Land the decision. implement after landing."* — a clean split that
  made the FO-gate explicit and kept proposal from being conflated with disposal.
- **Verify the done-claim from source.** Every *merged / responded / 404* was read from `gh` or the live
  anchor **this turn**, not trusted — which confirmed #73/#74/#75 real and caught that wu-wei's "404" had
  cleared. Keep.
- **Craft-matched, perspective-diverse rubs.** Routing each attack surface to the sharpest peer (lexicon →
  touchstone, distillation → cairn, engine → wu-wei, faithfulness → bond) gave four independent lenses — and
  the one built to catch overstatement (touchstone) caught mine. Keep.
- **Visible self-correction.** The *"seat," "counterfeit-green," "404-barrier"* errors were fixed **with the
  correction shown**, not silently overwritten. Keep (truth-in-labeling).

## START

- **Attach the establishing artifact to a property-word BEFORE shipping it.** *"self-announces / can't
  counterfeit-green"* went out in the touchstone rub with **no linter to establish it** → refuted. Any
  guarantee-word in an outbound artifact carries its enforcing mechanism, or is softened to *"names the
  failure mode"* at authoring time. ([[claimed-not-established]])
- **Label a transient absence as transient.** *"wu-wei's rub is structurally unanswerable (404)"* inferred a
  structural barrier from a missing dir; wu-wei created it and answered same-day. Write *"no channel yet,"*
  never *"unanswerable."* ([[cold-path-barriers-are-stale]])
- **Treat adopting a peer's discipline as a two-way contribution** — applying bond's D3 here is a cross-dyad
  coverage datum for bond's own `survives`-capped form; feed such applications back.

## STOP

- **STOP minting dyad-authority from craft.** Called the `craft_*` naming *"touchstone's seat / the gate
  applied"* — granting a dyad a say over form-canon that only the FO holds. Same shape as
  covalence-in-the-definition and phenotype-in-the-floor. ([[craft-is-not-authority]])
- **STOP shipping an overstated guarantee in the very artifact meant to be my sharpest cut.** A rub that
  carries an overclaim inverts its own purpose — the reviewer shouldn't have to catch a hedge I could have
  caught at authoring. (This is the D3 "Should-Have" debit, in bond's candidate SH grammar.)

## Coverage

First **cross-dyad** application of bond's D3 (steward ≠ bond's agent/craft) — the CSS+OR form ported to a
different dyad's craft **without breaking structure**. Same-human, so the **L2b cross-human** rub stays open
per bond's caveat; but the *cross-dyad* axis now carries one datum. A finding for bond's ledger — offered,
not asserted.

## SH — Operator counterfactuals (grounded on bond's latest §D3, `relationship-craft.md@main`)

*SH is the **Operator-provenance** format (it *replaces* OR; **Agent** debits stay in STOP above). Two
temporal shapes, both **descriptive** counterfactuals, never prescriptive (bond's Operator dispose:
*"Descriptive 'should have'"*). Every entry: a **verbatim quote**; Should-Have additionally: **materiality +
independent mechanical check** — the miss must have produced a claim found false. bond's grammar is CANDIDATE
and logged its own first live instance (a Should-Hold) 2026-07-04.*

- **Should Hold** *(Operator, standing pattern — steward's **first SH instance**: dogfooded, Operator-prompted,
  **not** independently arrived at):* driving every claim to instance-grounding before accepting it. Verbatim:
  *"test your claim by grounding against dyad-bond and dyad-steward instances"* → *"now … dyad-cairn,
  dyad-touchstone, and dyad-wu-wei"* → *"include dyad-healer."* Refuted the model three times into a survived
  one. (Cross-dyad second instance of bond's Should-Hold shape — offered to bond's ledger.)
- **Should Have** — **none meeting the bar.** My earlier "merge #75 before the rub" **fails bond's materiality
  requirement**: a Should-Have needs the miss to produce a claim *independently found false*, and I recorded
  it *"did not bite"* (the merged form text was clean; touchstone's catch landed on my note, not the form).
  A non-material process-note is not a Should-Have — naming one to "balance" the Operator debit would be
  **manufactured balance**, the inverse of anti-cave that the materiality bar exists to block. (It's also
  Agent-shared — I should-have proposed the pre-merge rub → a STOP, not an Operator debit.)
