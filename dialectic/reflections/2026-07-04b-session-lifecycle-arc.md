# Reflection (D3 · CSS+SH) — the session-lifecycle automation arc

*CSS = Agent; SH = Operator-provenance (Should-Have carries the materiality bar; both descriptive,
verbatim-quoted). Grounded on bond's live §D3 grammar (fetched this session). Landed by being committed;
no CTA.*

**Scope:** borrowing bond's Claude `SessionStart`/`SessionEnd` hooks → adopting `d-start` + retiring those
hooks for `agy`↔`claude` portability (the marker as the portable trigger).

## CONTINUE

- **Fresh-fetch the source before borrowing.** Every borrow this arc grounded on bond's *remote* first
  (`standup.sh`/`standdown.sh`/`settings.json`; then `GLOSSARY.md §d-*`). That's what surfaced that bond
  had **already** built `d-start` **and retired the hook** — I'd have missed both from memory.
- **Test every mode before wiring.** `standup` / `standup --hook` (JSON parsed) / `standdown` /
  `standdown --log`, and the exact hook invocation with `CLAUDE_PROJECT_DIR` resolved — before committing.
- **Dogfood the tool.** Ran `bin/standdown.sh` to drive *this* `d-reflect`, not a hand-rolled close.

## START

- **When borrowing a peer's mechanism, fetch its *latest* state first — it may have evolved past the thing
  you're copying.** bond had superseded the hook with the portable marker days before I "implemented" the
  hook; a full-lexicon fetch up front would have gone straight to `d-start`.

## STOP

- **Implementing a borrowed mechanism without checking whether the source already superseded it.** I built
  the Claude hooks, then retired them the next turn for portability — the spines were kept so it wasn't
  wasted, but a `GLOSSARY.md` fetch when first asked to "borrow the hooks" would have surfaced `d-start` +
  the portability model immediately. Grounding was partial (the two scripts) when it should have been the
  lexicon too.

## SH — Operator counterfactuals

- **Should Hold** *(Operator, standing pattern):* insisting on grounding via fresh remote fetch. Verbatim
  across the arc: *"reference its remote repo via fresh fetch"* · *"ground on latest by fetching from remote
  repo of dyad-bond's main."* This standing bar is exactly what caught the already-evolved `d-start` and the
  retired hook — it converts stale-cache borrowing into grounded borrowing.
- **Should Have** — **none meeting the bar.** The build-then-retire two-step (hooks last turn → marker this
  turn) produced **no claim found false** — the spines were kept, the marker supersedes cleanly, nothing bit.
  Per the materiality bar it is not a Should-Have; and the two-step reads as a deliberate build-to-understand
  → generalize progression, not a miss. Naming one would be manufactured balance.

## Coverage

Second cross-dyad adoption of a bond `d-*` discipline (after `d-reflect`) — a coverage datum for bond's
`d-*` lexicon on the cross-dyad axis. Same-human; the cross-human rub stays open.
