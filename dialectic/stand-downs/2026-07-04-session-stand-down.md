# Session stand-down — 2026-07-04 (the G0-ABC → operate-model → lifecycle-disciplines session)

> Triggered by `d-reflect`. `d-reflect ⊂ stand-down`: the reflection is in
> `dialectic/reflections/2026-07-04-g0-abc-operate-model.md` (+ SH) and `…-2026-07-04b-session-lifecycle-arc.md`.

## Closed this session (all landed + merged)

- **bond's G0 form-canon package, end-to-end** — refereed across a 6-dyad falsification campaign, then:
  - **F1** — the Invisible-Elicitor/rub-all block excised from the form `AGENT.md` (Commons **PR #74**, merged).
  - **single-home** playbook — bond's PR **#73**, merged → `commons/library/single-home/`.
  - **The DIP operate-model restructure** — form `AGENT.md` restructured around **Contract / Operating-policy /
    Craft** (`craft_telos` foregrounded; Operating-policy dimension added; #5 split; `NOT_YET_WORN`). Commons
    **PR #75**, merged `ce2bf9a`. Full record: `dialectic/g0-abc-tightening.md`.
- **Inter-dyad peer contest on #75** — 4/4 rubs returned: bond faithful · cairn corroborated (cross-dyad
  verification-debt) · touchstone corroborated + 1 real catch (`NOT_YET_WORN` overstatement, fixed) · wu-wei
  corroborated. Two follow-up nodes adopted (below).
- **Disciplines adopted from bond (fresh-fetched):** `d-reflect` (CSS+SH reflect-form) · `d-start` +
  Start-Session Discipline · `d-land`. Session lifecycle now marker-driven (`dialectic/session-lifecycle-discipline.md`).
- **Tooling:** `bin/standup.sh` + `bin/standdown.sh` (the d-start/d-reflect spines). Claude session hooks
  **retired** for portability — `--hook`/`--log` kept dormant.

## FO-gate
- None open. All Commons PRs (#73/#74/#75) merged; commons pin at `ce2bf9a`.

## OPEN — resume at next stand-up
- **Follow-up nodes (READY):** `craft_notyetworn_linter` (touchstone's counter — make the sentinel
  enforceable) · `fr_bundle_evidence` (cairn's Mason-hat — bundle quoted passage + blob SHA in FRs).
- **Threads (not nodes):** the **tco non-inheriting test** (clean-independence falsifier for single-home + the
  R3 contract-convergence) · optional **healer rub** on #75 (the domain-craft lens) · **R6** (`1+1>2` operative
  tenet vs `1+1=3` banner — separate, undisposed).
- **Rub coverage datums owed to bond's ledgers:** steward's cross-dyad applications of D3 (Should-Hold) and
  the `d-*` lexicon — offered, not asserted (same-human).
- **Persistent:** 3 unreachable DM sources (krishna · personal-growth-ai · shakti — `dharan31chase`/private).

## LIFECYCLE — how the NEXT session opens
- **No auto-stand-up.** The Claude SessionStart hook is retired; the Operator types **`d-start`** to open
  (runs `bin/standup.sh` + the Start-Session Discipline: ground · re-arm daemons · pull the 4 surfaces · NBA).
- Session-only daemons (DM-inbox · commons-PR · peer-review) are **not armed** — re-arm at `d-start` (session-pin).

## State at stand-down (verified, not cached)
- `main`: local == origin (**SYNCED**) at `b1b6c73` (incl. the Operator's `defaultMode:auto` settings commit).
- Working tree: 2 known-noise paths only — commons `__pycache__` (untracked; `commons_gitignore` node fixes)
  + `sandbox/apply_automode_allow.py` (pre-existing). No uncommitted session work.
- Frontier: idle (WIP-N=1: none); 15 READY. `bin/frontier.py --check`: in sync.
