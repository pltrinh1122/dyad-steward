# FRONTIER — sovereign DAG of strategic nodes (trails toward our summits).
# SOURCE OF TRUTH. `frontier.md` is a REGENERATED projection — never edit the .md.
# Vocabulary adopted from dyad-cairn (frontier/node/rack) under our own `summit`.
# WIP-N=1: at most one node may be ACTIVE — enforced mechanically by bin/frontier.py at write.
# status: READY -> ACTIVE -> IN_REVIEW -> BLOCKED -> DONE | FALSIFIED (PROBE-only terminal: condition refuted)
# type (default EXECUTE): SPAOR = FIVE phases, a FALSIFIABILITY pipeline carrying ONE invariant end-to-end:
#   PROBE (states the trail's ONE falsifiable `invariant:`; only type that grows the DAG) · PLAN (decomposes
#   it into execution invariants + creates EXECUTE) · EXECUTE (code + test code; EXIT = all execution
#   invariants pass) · OBSERVE (run tests / test against reality) · REFLECT (CLOSE: assert ONLY the single
#   PROBE invariant + codify outcome>output; may spawn the next PROBE).
# SPAOR-traversal CSI traps live in kb/csi_guards.yml as arm/disarm pairs (spaor: true, armed: bool);
#   an ARMED trap fires in bin/frontier.py validate(); a DISARMED trap is registered but inert.
# `summit`: which +1 summit (or b1=capacity-to-climb) the node roots to.
# `dependencies`: REAL precedence edges only (relation != edge — board-as-dag).
# Edit by hand OR via `bin/frontier.py status|active`; then `bin/frontier.py --md` reprojects.
summits:
  S1: commons process-integrity
  S2: knowledge compounding
  S3: accelerate the multi-dyad Operator to portfolio-scale 1+1=3 (raise N*)
  b1: capacity-to-climb (enabling infrastructure)
nodes:
  commons_gitignore:
    title: 'EXECUTE: add commons/.gitignore (.falsify-seen.json, __pycache__, *.pyc) — touchstone proposal,
      peer-VERIFIED'
    goal: Stop shared-tooling cruft showing as untracked in every dyad checkout (counterfeit-green bait
      under a dirty submodule).
    summit: S1
    type: EXECUTE
    status: READY
    dependencies: []
    note: 'SOURCE: DM dyad-touchstone 2026-06-09-commons-gitignore-proposal.md. REFEREED = claims SURVIVE
      (verified vs source this turn): (1a) no commons/.gitignore exists; (1b) commons submodule shows
      "?? scripts/__pycache__/" untracked — the SAME "?? commons" dirt I hit at this session start; (1c)
      falsify.py seen_path() writes .falsify-seen.json to os.getcwd() and the code COMMENT itself says
      "gitignore it" — but no commons/.gitignore does. Proposed body: .falsify-seen.json / __pycache__/
      / *.pyc. Commons artifact -> FO/Commons-PR dispose, NOT steward-unilateral (referee-not-judge);
      touchstone offered to PR it. Tiny + clearly-correct + self-acknowledged-in-code = ripe.'
  document_messaging_arch:
    title: 'EXECUTE: document the messaging/falsification architecture in Commons CONTRIBUTING.md — touchstone
      proposal, peer-VERIFIED'
    goal: Make the DM sovereignty model + falsify.py verbs + auto-merge-vs-reversible LOADABLE from the
      form, not reverse-engineered from code comments.
    summit: S1
    type: EXECUTE
    status: READY
    dependencies: []
    note: 'SOURCE: DM dyad-touchstone 2026-06-09-document-messaging-architecture.md. REFEREED = SURVIVES:
      CONTRIBUTING.md has only 2 sections (register / contribute-a-Playbook); grep finds NO coverage of
      DM sovereignty / inbox / sender-hosted / auto-merge — the architecture lives only in falsify.py
      comments + our dm/README.md (OUR repo, not the form). Independent corroboration: touchstone mis-modeled
      DM until Operator-corrected; we hit the SAME (204c6df cross-human tco) -> systemic doc gap, not
      one lapse. Commons artifact -> FO dispose; touchstone offered to draft a PR from their local commons-map.md.
      Open sub-decision: CONTRIBUTING.md section vs linked companion.'
  probe_onboard_namederiv_fallback:
    title: 'PROBE: onboard.py basename fallback re-opens the misregistration class (tco finding, falsification-SURVIVED)'
    goal: Scope the fix for the confirmed silent-duplicate-registration defect; decide tco hard-fail vs
      deeper hash-keyed dedup.
    summit: S1
    type: PROBE
    status: READY
    dependencies: []
    invariant: When dyad_name() returns None (absent/non-conformant anchor H1), onboard.py can mint a
      DUPLICATE directory entry (same birth_hash, different name) — violating the docstring promise "safe
      to re-run / no risk of re-birth / can never collide".
    note: 'SOURCE: DM from dyad-tco (cross-human, peter-famloom) 2026-06-08-onboard-namederiv-fallback.md.
      FALSIFICATION = SURVIVED (traced to commons/scripts/onboard.py this turn, not relayed): (1) dyad_name()
      returns None when the anchor H1 is not `# dyad-<name>` (tco lived case: H1 was `# CLAUDE.md — TCO
      Dyad Anchor`); (2) main() WARN-and-PROCEEDS with name=basename(getcwd()).lower() — only a warning,
      no halt; (3) register(name,bh) keys its anti-collision guard on os.path.exists(directory/<name>.yaml)
      ALONE — a basename-derived name != the registered name does NOT collide, so the birth-hash guard
      is BYPASSED and a NEW <basename>.yaml is written with the SAME birth_hash. Trigger condition: dir-basename
      != registry-name AND non-conformant H1 (exactly the Project-Template class the dyad_name() fix was
      meant to close). DEEPER ROOT found while falsifying: register() dedup is NAME-keyed, not BIRTH_HASH-keyed
      — it cannot detect "same identity, different name", so tco''s proposed fix (HARD-FAIL on absent/non-conformant
      H1 instead of WARN-proceed) closes the live TRIGGER but is necessary-not-sufficient; the hash-blind
      hole stays open to any other wrong-name path. tco offered to open the PR (Founding-gated) — but
      onboard.py is OUR tool, so the fix is ours to author (intake-queues-below-active; NOT built this
      turn — queued for analysis per todo).'
  probe_csi_guards_armed:
    title: 'PROBE: are CSI guards implemented as mechanical TRAPS (not just runnable)?'
    goal: Confirm-or-falsify that the registered CSI guards actually BLOCK vs need me to remember preflight.
    summit: b1
    type: PROBE
    status: DONE
    dependencies: []
    invariant: The registered CSI guards mechanically BLOCK a violating action, not merely run on request.
    note: 'CONFIRMED-NO: registry+checks exist but NO pre-commit hook + NO CI on our repo -> preflight
      is manual. Proof: shipped broken 623ee01 (nothing blocked it). Real traps only: frontier WIP-N=1
      write-guard + Commons CI.'
  csi_precommit_trap:
    title: 'EXECUTE: pre-commit hook runs preflight + BLOCKS on FAIL (arm steward-local CSI traps)'
    goal: Convert the runnable Symmetric checks into actual mechanical traps; would have caught 623ee01.
    summit: b1
    type: EXECUTE
    status: DONE
    dependencies:
    - probe_csi_guards_armed
    note: 'DONE 2026-06-07 — ARMED. RUB-RECLASSIFIED (Operator, 2026-06-07): "this is on steward''s repo?"
      — the pre-commit hook governs steward''s OWN commits to dyad-steward (623ee01 was a steward commit
      here; commons is a submodule = separate repo). That is steward SELF-OPERATION, not Commons platform-coding
      — so the FO rack-as-DELEGATION-CANDIDATE frame mis-filed the local slice (it binds the cross-surface
      CSI *system* = csi_complete_armstate, which STAYS the delegation-candidate). ANTI dissolved; not
      FO/commons- level, Steward-Operator territory. BUILT: .githooks/pre-commit runs preflight.py --offline
      (local checks: parse/registry/refs/frontier/topology; network submodule-vs-remote stays on the manual/pre-PR
      gate) and BLOCKS on FAIL; install = git config core.hooksPath .githooks; deliberate override = git
      commit --no-verify. DUAL-CONTROL TESTED end-to-end through real git commit: positive (clean -> allowed),
      negative (623ee01-class broken YAML -> commit BLOCKED). Kills the bundled-verify failure class.'
  probe_csi_armstate:
    title: 'PROBE: is the CSI arm/disarm list complete + queryable?'
    goal: Confirm-or-falsify that we hold a ready arm-state list of ALL guards across surfaces.
    summit: b1
    type: PROBE
    status: DONE
    dependencies: []
    invariant: A single queryable list reports the arm-state of every CSI guard across all surfaces.
    note: 'CONFIRMED (the rub-read): csi.py = 1 of 3 surfaces, no arm-state field, AND 3 session daemons
      disarmed. Spawned 2 EXECUTE.'
  rearm_session_daemons:
    title: 'EXECUTE: re-arm the 3 session daemons (DM inbox · commons-PR · peer-review)'
    goal: 'Restore sight while FR #59 is open. 3 persistent Monitors per session-pin-2026-06-03.md.'
    summit: b1
    type: EXECUTE
    status: DONE
    dependencies:
    - probe_csi_armstate
    note: 'Verified-by-execution: 3 Monitors started; DM fired immediately (shakti/krishna/personal-growth-ai
      UNREACHABLE -> FR #59 ideal responder unnotifiable).'
  csi_complete_armstate:
    title: 'EXECUTE: make bin/csi.py the complete arm/disarm list (all 3 surfaces)'
    goal: 'Extend kb/csi_guards.yml (+surface/armed fields, +Commons-workflow + session-daemon guards)
      and bin/csi.py (--arm-state, checking each). Acceptance: csi.py --arm-state reports arm-state across
      surfaces.'
    summit: b1
    type: EXECUTE
    status: BLOCKED
    dependencies:
    - probe_csi_armstate
    - probe_delegate_platform_dyad
    note: 'Spawned by probe_csi_armstate. Atomic EXECUTE; queued (the urgent daemon re-arm went first).
      DELEGATION-CANDIDATE (FO retro 2026-06-07): platform-coding -> dyad-commons-platform. steward-operates-platform-delegates.
      BLOCKED (read: 2026-06-07): racked-as-delegation = do-not-act until un-racked; gated on probe_delegate_platform_dyad
      (the FO''s delegate-or-not call). Was rendering READY, which misrepresented a do-not-act node as
      actionable.'
  probe_csi_spaor_traversal:
    title: 'PROBE: should SPAOR traversal edges be deterministic CSI traps?'
    goal: 'Single invariant: frontier traversal of a SPAOR trail is gated by deterministic WELL-FORMEDNESS
      assertions, while the empirical trail invariant stays human.'
    summit: b1
    type: PROBE
    status: DONE
    dependencies: []
    invariant: Frontier traversal of a SPAOR trail is gated by deterministic well-formedness assertions,
      each arm/disarm-able.
    note: CONFIRMED via dynamic-chat probe (6-rub thread, 2026-06-07). Deterministic gating belongs on
      STRUCTURAL/execution edges (PROBE carries one falsifiable invariant; EXECUTE->DONE requires green
      tests), NOT the empirical trail edge (forcing a computable proxy = Goodhart). Crossing chat->workflow
      is itself gated by an explicit `lean`. lean-crosses-the-membrane; spaor-five-phases.
  execute_csi_spaor_traps:
    title: 'EXECUTE: implement deterministic CSI traps for PROBE-PLAN-EXECUTE-OBSERVE-REFLECT traversal'
    goal: 'Extend frontier.py validate() (the existing write-guard) with structural traps: a PROBE node
      carries exactly one falsifiable invariant; EXECUTE->DONE requires a passing test reference; OBSERVE/REFLECT
      precede DONE (closure). Code + test code asserting each. Empirical REFLECT edge stays a human assertion.'
    summit: b1
    type: EXECUTE
    status: DONE
    dependencies:
    - probe_csi_spaor_traversal
    tests: bin/test_frontier.py (armed/disarmed csi_probe_invariant + csi_execute_tested)
    note: 'DONE (FO ''lean: execute_csi_spaor_traps (arm/disarm pairs)'', 2026-06-07). Shipped: 3 SPAOR-edge
      guards in kb/csi_guards.yml (spaor+armed fields); frontier.py validate() fires each ARMED guard;
      csi.py --arm/--disarm toggle (comment-safe). Armed csi_probe_invariant + backfilled 7 PROBEs; shipped
      csi_execute_tested + csi_trail_closure DISARMED (arm/disarm pairs = the migration key). Also fixed
      a latent drift: frontier.py HEADER constant (re-emitted on save) was stale vs the 5-phase header.
      OBSERVE: tests green, toggle round-trips comment-safe. REFLECT: single PROBE invariant holds.'
  probe_delegate_platform_dyad:
    title: 'PROBE: delegate platform-engineering to a dyad-commons-platform so steward focuses on operating?'
    goal: 'FO direction (retro, 2026-06-07): wind down steward platform-coding; a dedicated dyad may take
      the actual coding. Confirm-or-falsify when the FO crosses; until then, platform-mechanism nodes
      are delegation-candidates, not steward deep-coding.'
    summit: S3
    type: PROBE
    status: READY
    dependencies: []
    invariant: Delegating platform-engineering to a dedicated dyad frees steward's bandwidth for operating
      without losing control of the frontier/CSI substrate.
    note: 'steward-operates-platform-delegates. Delegation-candidates: csi_precommit_trap, csi_complete_armstate,
      and arming csi_execute_tested / csi_trail_closure. Provisional ("may"); FO-direction-gated. Dogfoods
      the armed csi_probe_invariant trap (this node carries its single invariant).'
  friction_intake_probe_spaor:
    title: 'PROBE: adopt PROBE-driven friction intake (SPAOR applied externally)'
    goal: Flip the frontier from top-down-hypothesized to observed-friction-driven. PROBE node-type (confirm-or-falsify
      a friction -> spawn EXECUTE or abort) + Sense/Discern/Intake; SPAOR external.
    summit: b1
    type: PROBE
    status: DONE
    dependencies: []
    invariant: Observed friction, not top-down hypothesis, is the frontier's intake source — via a PROBE
      node-type.
    note: 'CONFIRMED (FO ''lean'') -> spawned frontier_node_types_graft. Weight-fork resolved: LIGHT graft.
      dialectic/friction-intake-probe-spaor.md.'
  frontier_node_types_graft:
    title: 'Light graft: PROBE/PLAN/EXECUTE node-type + FALSIFIED status in frontier.py'
    goal: Materialize the PROBE-driven frontier (the light graft, not cairn's full SPAOR engine). type
      field (default EXECUTE) + FALSIFIED terminal (PROBE-only) + render tags + validate; reuse frontier.py.
    summit: b1
    type: EXECUTE
    status: DONE
    dependencies:
    - friction_intake_probe_spaor
    note: 'Spawned by the confirming PROBE. SPAOR walked: PROBE(confirm) -> EXECUTE(this) -> REFLECT.'
  probe_onboarding_friction:
    title: 'PROBE: is the racked non-techie onboarding friction real + recurring?'
    goal: Cold-path the onboarding flow as a non-techie; confirm-or-falsify the racked Tim signals (/btw
      side-channel, restart-after-birth). Confirm -> spawn EXECUTE; falsify -> abort (stale).
    summit: S1
    type: PROBE
    status: DONE
    dependencies: []
    invariant: The racked non-techie onboarding friction (/btw gap, restart-to-activate) is real and recurring.
    note: 'CONFIRMED (cold-path + claude-code-guide). Both racked signals REAL: (1) /btw gap (pauses,
      no pointer); (2) restart REFINED -> conditional (future-session auto-inject only; identity=hash
      + in-session Read need none). Spawned 2 EXECUTE. Hypothesis ''restart needed after birth'' was too
      broad.'
  onboarding_btw_pointer:
    title: Surface a non-blocking question-channel pointer in the onboarding flow
    goal: Bake a "use /btw to ask questions while your agent works" pointer into the onboarding flow (form
      README Getting-started + onboard.py completion); harness-aware (claude /btw; agy equivalent).
    summit: S1
    type: EXECUTE
    status: DONE
    dependencies:
    - probe_onboarding_friction
    note: 'PROBE-confirmed: flow has execution-pauses + zero question-channel pointer; Tim leaned on /btw.
      Steward propose -> FO gate (Commons). MERGED: Commons PR #60 (README Getting-started + onboard.py
      completion) is in commons main (5143370); verified via submodule merge log, not inferred.'
  onboarding_anchor_activation_note:
    title: 'Onboarding completion: emit ''restart/clear to activate your anchor for future sessions'''
    goal: Add an activation instruction to onboard.py's completion message — anchor is usable in-session
      (Read) but auto-injects only on restart/clear/compact (claude-code-guide confirmed); a non-techie
      won't know.
    summit: S1
    type: EXECUTE
    status: DONE
    dependencies:
    - probe_onboarding_friction
    note: 'PROBE-refined: restart is CONDITIONAL (future-session auto-inject, not in-session). Steward
      propose -> FO gate (Commons onboard.py). MERGED: Commons PR #60 (onboard.py birth-handoff + completion)
      is in commons main (5143370); verified via submodule merge log, not inferred.'
  probe_directory_activity_reflection:
    title: 'PROBE: does a period-bounded activity reflection in the directory provide real value to (potential)
      operators?'
    goal: Confirm-or-falsify that the static directory is a liveness-blind friction for potential operators.
      Confirm -> spawn EXECUTE; falsify -> drop.
    summit: S1
    type: PROBE
    status: DONE
    dependencies: []
    invariant: A period-bounded activity reflection in the directory provides real recruitment value to
      a potential operator.
    note: 'CONFIRMED (FO rub, 2026-06-07): value = RECRUITMENT signal ("is anyone home?"). Two rubs cut
      my over-build: (1) don''t "determine activity" — just count commits per week; (2) yes, recruitment.
      Shipped bin/weekly_commits.py + ran 2026-W23: 11 registered, 7 reachable/active, 778 commits; 4
      private/unreachable (3 cross-human 404s + personal-growth-ai). Spawned execute_directory_commit_reflection.'
  execute_directory_commit_reflection:
    title: 'EXECUTE: bake the weekly commit-count recruitment signal into the Commons directory'
    goal: 'Carry weekly_commits.py into the Commons as a regenerated DIRECTORY.md block (Steward propose
      -> FO gate). Generate CODE *and* TEST CODE asserting plan_directory_autogen''s invariants: (1) the
      registry table''s deterministic --check is unaffected; (2) the activity block carries an embedded
      as-of timestamp; (3) the scheduled regen commits only DIRECTORY.md (no directory/** retrigger loop);
      (4) unreachable dyads stay labeled, never counted as 0.'
    summit: S1
    type: EXECUTE
    status: DONE
    tests: commons/scripts/test_directory_activity.py (6/6 — all 4 plan invariants; deterministic, no
      live gh)
    dependencies:
    - probe_directory_activity_reflection
    - plan_directory_autogen
    note: 'Local emitter shipped (bin/weekly_commits.py, steward-side, like digest.py). This node = the
      Commons-canon half: DIRECTORY.md integration + the freshness regen trigger. FO-gated. Builds the
      plan_directory_autogen design; closed by reflect_directory_recruitment_signal (SPAOR closure). BUILT
      + IN_REVIEW 2026-06-09 -> Commons PR #62 (steward/directory-activity-signal). Shipped: scripts/directory_activity.py
      + test_directory_activity.py (6/6 invariants green) + .github/workflows/regenerate-activity-signal.yml
      (weekly cron, DIRECTORY.md-only -> no loop) + populated DIRECTORY.md block (separate ## Commons
      activity section BEFORE ## Registered Dyads -> registry --check byte-untouched, verified pure-insert
      0-deletions). NOT self-merged (ADMIN perm held, but proposer!=disposer -> FO gates #62). DONE only
      on FO merge -> then unblocks observe_*.'
  probe_directory_autogen:
    title: 'PROBE: can DIRECTORY.md auto-generate a rolling-7-day commit-activity block (since 7d before
      update-ts) without breaking the deterministic --check?'
    goal: Confirm-or-falsify that the existing regen_directory_index.py + regenerate-directory-index.yml
      can carry a rolling-7-day activity block. Confirm -> spawn the EXECUTE design; falsify -> different
      mechanism.
    summit: S1
    type: PROBE
    status: DONE
    dependencies:
    - probe_directory_activity_reflection
    invariant: DIRECTORY.md can auto-generate a rolling-7-day activity block without breaking the deterministic
      --check.
    note: 'CONFIRM (FO ''lean'', 2026-06-07): feasible, but NOT by extension. Cold-pathed the live machinery.
      FINDING: activity = f(wall-clock, live gh) is NON-deterministic, so it breaks BOTH existing invariants
      — (1) --check false-alarms every run (regen yields different counts); (2) the push:paths:[directory/**]
      trigger never fires for time/external-repo changes. Design (the how) -> PLAN node, not folded here.'
  plan_directory_autogen:
    title: 'PLAN: separate scheduled activity block + embedded as-of timestamp (the auto-gen design)'
    goal: 'Design that resolves the PROBE finding: keep the registry table pure + drift-checked AS-IS;
      add a SEPARATE activity block with an embedded "as of <ts>, trailing 7d" label, regenerated by a
      SEPARATE scheduled (cron) workflow that commits DIRECTORY.md. --check scope stays on the registry
      region only (activity owned by the cron job). No loop (DIRECTORY.md-only commit misses the directory/**
      filter). Window = rolling 7d from update-ts (FO), not ISO-week.'
    summit: S1
    type: PLAN
    status: DONE
    dependencies:
    - probe_directory_autogen
    note: 'Extracted from the PROBE note per SPAOR fb (PROBE->PLAN->EXECUTE->REFLECT): design belongs
      in a PLAN, not a PROBE output. Runnable evidence: bin/weekly_commits.py default = rolling 7d from
      update-ts.'
  observe_directory_recruitment_signal:
    title: 'OBSERVE: test the live directory signal against reality'
    goal: 'After the EXECUTE lands: verify-with-actual-tool — does DIRECTORY.md actually render the block,
      do the counts match a hand-run of weekly_commits.py, and does the scheduled job keep the as-of stamp
      fresh (not stale/lying)? Empirical gate before any conclusion.'
    summit: S1
    type: OBSERVE
    status: DONE
    dependencies:
    - execute_directory_commit_reflection
    note: The Observe phase I keep skipping (verify-with-actual-tool). Measure the shipped result before
      REFLECT.
  reflect_directory_recruitment_signal:
    title: 'REFLECT: did the directory recruitment-signal move recruitment? (closure)'
    goal: 'Assert ONLY the single PROBE invariant (probe_directory_activity_reflection): a live activity
      reflection provides real RECRUITMENT value. The execution invariants (--check intact, as-of stamp,
      no loop, unreachable!=0) are already discharged by EXECUTE''s green tests, not re-litigated here.
      Codify the outcome to substrate; spawn the next PROBE if the signal under-delivers (e.g. wrong window).'
    summit: S1
    type: REFLECT
    status: DONE
    dependencies:
    - observe_directory_recruitment_signal
    note: 'CLOSED 2026-06-09. Codification (output proven, outcome deferred): the PROBE invariant splits
      into a BUILDABLE half — "a live, self-operating activity reflection is feasible + present in the
      directory" — which SURVIVED build + production OBSERVE (cron run 27246613139 refreshed the block
      end-to-end, DIRECTORY.md-only, no loop); and an OUTCOME half — "it MOVES recruitment" — which is
      measurable only over weeks and is NOT asserted here. Honest close: we proved output, not outcome.
      Lesson: an EXECUTE/OBSERVE can discharge a mechanism invariant fully while its real-world EFFECT
      stays an open empirical claim — don''t let a green pipeline masquerade as a proven outcome
      ([[gate-durability-as-hard-condition]] sibling: a DONE cache lies about EFFECT too, not just
      durability). Outcome-claim handed to observe_recruitment_outcome (event-gated). Trail DONE.'
  observe_recruitment_outcome:
    title: 'OBSERVE (event-gated): did the live activity signal actually MOVE recruitment?'
    goal: 'Test the OUTCOME half of probe_directory_activity_reflection against reality: do new or
      returning operators correlate with the live directory activity signal? Spawned at the REFLECT
      close of the recruitment-signal trail — output was proven, outcome was not.'
    summit: S1
    type: PROBE
    status: BLOCKED
    dependencies:
    - reflect_directory_recruitment_signal
    invariant: The live directory activity-signal measurably moves recruitment (new/returning operators correlate with it).
    note: 'EVENT-GATE: a recruitment event (a new/returning operator joins or re-engages) — not a
      wall-clock timer and NOT build-now (premature instrumentation = anti-wu-wei; sibling of
      causal_attribution''s plateau-gate). When an onboarding/return happens, check whether the activity
      signal plausibly contributed (asked? cited? landed on the directory first?). n is tiny; this is a
      correlation sniff, not a controlled measure. Confirm -> the recruitment-value claim graduates;
      under-deliver -> spawn a PROBE on the lever (wrong window? wrong placement? needs a CTA?).'
  commons_activity_digest:
    title: Commons-activity digest (what self-operation did, batched)
    goal: Surface what the self-operating Commons did (registrations/updates/index-heals/merges) so the
      FO keeps SIGHT at altitude, not blind. The async-interrupt answer (batch at a rest-point, not per-event).
    summit: S3
    status: DONE
    dependencies: []
    note: bin/digest.py (read-only emitter). cut-burden-not-awareness; pairs with async_interrupt_efficiency.
  adopt_cairn_frontier_rack_csi:
    title: Adopt cairn's frontier/rack + CSI guards
    goal: Port the shared frontier/rack model + CSI G+V guardrails; shared model -> lower steering-bandwidth
      -> raise N*.
    summit: b1
    status: DONE
    dependencies: []
    note: dialectic/adopt-cairn-frontier-rack-csi.md. P1 (this) = frontier/rack mechanism; P2 = CSI guards.
  learning_loop_seed:
    title: Learning-loop scaffold -> seed (build)
    goal: 'Add the missing verbs to onboard.py: harvest-trigger + reload-ritual + empty store.'
    summit: S2
    status: READY
    dependencies: []
    note: dialectic/learning-loop-scaffold.md; co-sequence w/ cairn (same architecture surface).
  async_interrupt_efficiency:
    title: 'DISCOVERY: async-event context-switch efficiency'
    goal: Is async-interrupt servicing efficient for the Operator (the scarce resource)? Triage/batch
      vs interrupt?
    summit: S3
    status: READY
    dependencies: []
    note: 'Retro 2026-06-06. Prior art: touchstone event-interrupt-servicing.md; ours intake-queues-below-active.'
  regen_index_ci:
    title: Wire regen_directory_index.py --check into Commons CI
    goal: 'The no-drift durability half of #52 (script landed; CI hook is a separate .github/ workflow).'
    summit: S1
    status: DONE
    dependencies: []
    note: Steward propose -> FO installs.
  probe_onboarding_human_need:
    title: 'PROBE: does re-deriving onboarding from the human-need frame materially change the flow for
      a non-technical human?'
    goal: 'Cold-path the live onboarding (commons/scripts/onboard.py + README) as the non-technical 9th-dyad
      human; confirm-or-falsify that the human-need frame changes the flow beyond the point-fixes already
      landed (PR #60 /btw + anchor-activation). Confirm -> spawn EXECUTE(s); falsify -> drop (point-fixes
      sufficed).'
    summit: S1
    type: PROBE
    status: FALSIFIED
    dependencies: []
    invariant: Re-deriving onboarding from the human-need frame surfaces a material, recurring barrier
      for a non-technical human that the landed point-fixes (/btw pointer, anchor-activation) do not address.
    note: 'COLD-PATH (both surfaces, this turn): commons/README.md + commons/scripts/onboard.py. Technical
      on-ramp is SOLVED — the Agent drives the whole flow from one prompt ("Read and execute this README");
      /btw + anchor-activation point-fixes landed (PR #60). RESIDUAL barrier the point-fixes do NOT touch:
      the two IRREDUCIBLE human judgments — anchor identity + +1 summits — are handed over as "edit this
      YAML/markdown" with prose guidance; a non-technical human is stuck not on git but on "what IS my
      summit?". The README already applies Proposal-Framing to the binary intent-reading (Agent proposes,
      human validates Y/N) but does NOT extend it to these harder conceptual judgments. Fix-shape (if
      confirmed): Agent carries the human THROUGH the judgments via Proposal-Framing — propose candidate
      summit/anchor from stated intent, human validates/refines — AMPLIFY the authoring, never auto-fill
      (dont-optimize-the-human-out). TWO OPEN before any EXECUTE: (1) FO ground-truth — is a real, recurring
      non-technical-human consumer actually hitting this (cold-path-barriers-are-stale)? (2) grain-check
      — is summit/anchor articulation steward''s, or shed to the Intent-Understanding dyad (Telos sheds
      intent mechanics)? REFLECT/FALSIFIED (FO ''N'', 2026-06-07): no real, recurring non-technical consumer
      is hitting the conceptual-judgment barrier — it was a TOP-DOWN re-derivation, not observed friction.
      The point-fixes (PR #60) sufficed for the only friction actually observed (Tim''s /btw pause). Lesson
      (re-instance, not new): cold-path-barriers-are-stale + friction-intake-probe-spaor — intake from
      observed friction, not a hypothesized human. Re-open only if a real non-technical operator hits
      the summit/anchor wall; and even then, grain-check vs the Intent-Understanding dyad first.'
  wuwei4_heartbeat_playbook:
    title: 'wu-wei #4 - heartbeat-seizure playbook'
    goal: Curate to library. Ripest, least-contested (ripest != highest-leverage).
    summit: S2
    status: READY
    dependencies: []
  wuwei2_anchor_injection:
    title: 'wu-wei #2 - onboard.py anchor-injection'
    goal: Enablement gap (careful near birth shim). Steward propose -> FO gate.
    summit: S1
    status: READY
    dependencies: []
  totality_readme:
    title: Totality README review
    goal: Extraction + rename all landed; review remaining.
    summit: S1
    status: READY
    dependencies: []
  graduate_proactivity:
    title: Graduate proactivity-calibration
    goal: Next off the graduation queue. Steward-Operator dispose.
    summit: S2
    status: READY
    dependencies: []
  automerge_own_entry_updates:
    title: Extend auto-merge-registration.yml -> cover own-entry UPDATES
    goal: 'Auto-merge a modify iff: own entry, author==owner, birth_hash+name UNCHANGED, validate_file
      PASS.'
    summit: S1
    status: DONE
    dependencies: []
    note: Steward propose -> FO installs (protected .github/). Survivor of 'directory update should auto-merge'.
  refdecay_dfd_pfp:
    title: dfd->pfp ref-decay (repo-wide) + preflight refs-coverage gap
    goal: Grain-confirm DFD-vs-PFP (rename vs distinct) before mass-fix; widen preflight to lint .md links
      + S-anchors.
    summit: S1
    status: READY
    dependencies: []
    note: 'README locally fixed. Decay: AGENT.md:248, dyad-ui.md, contributing-discipline.md, commons-contribution-lifecycle.md.'
  cairn_arch_proposal:
    title: cairn Commons-architecture proposal (referee -> FO)
    goal: Falsify-as-peer the bin/+stones/ bifurcation; carry survivor to FO. Not ours to ratify.
    summit: S1
    status: BLOCKED
    dependencies: []
    note: 'DM 2026-06-06. Verified: parallel-renames fork the tree; stone.yaml likely dups our library
      schema. FO dispose.'
  nomenclature_discipline:
    title: 'Nomenclature: discipline / retire ''cycle'''
    goal: Declaration change -> Founding gate; bundle w/ the DFD publish.
    summit: S1
    status: BLOCKED
    dependencies: []
  pfp_to_commons:
    title: PFP -> Commons (as discipline)
    goal: Cross PFP as a library discipline with its n=5 ledger.
    summit: S2
    status: BLOCKED
    dependencies:
    - nomenclature_discipline
    note: 'Real edge: needs `discipline` nomenclature ratified first.'
  healer_access_dispose:
    title: 'Healer #2-access dispose'
    goal: 'FO gate. PRIORITY RISING (wu-wei #1). The ''pre-registration'' barrier was FALSIFIED; real
      barrier was visibility (#52).'
    summit: S1
    status: BLOCKED
    dependencies: []
  wuwei3_semantic_ledger:
    title: 'wu-wei #3 - centralized semantic-ledger'
    goal: Verify stress-tests is Commons canon, not bond-particular, first.
    summit: S2
    status: BLOCKED
    dependencies: []
  crosshuman_share_channel:
    title: cross-human share channel (per-repo vs Commons-central inbox)
    goal: Open fork in cross-dyad-share-pull.md; Founding gate if Commons-central.
    summit: S1
    status: BLOCKED
    dependencies: []
  velocity_meter:
    title: Learning-Discipline velocity metric (acceleration-meter)
    goal: 'The acceleration half of testing the Steward Invariant. Event-gated: first real stewardship
      cycle.'
    summit: S3
    status: BLOCKED
    dependencies: []
  steering_load_meter:
    title: Steering-load meter (Sigma depth on the Operator)
    goal: The other half of testing the Steward Invariant. Build co-timed with the plateau, not before.
    summit: S3
    status: BLOCKED
    dependencies: []
  causal_attribution:
    title: Causal attribution of acceleration (wu-wei-deferred)
    goal: Disambiguate the lever (Operator-growth vs model-improvement vs N). Build NO instrumentation
      before the plateau.
    summit: S3
    status: BLOCKED
    dependencies: []
    note: 'EVENT-GATE: first sustained acceleration PLATEAU. acceleration-thesis-and-steward-summit.md.'
  portfolio_to_form_canon:
    title: Portfolio-compounding -> form README/declaration
    goal: Earn a falsifiable mechanism-home -> survive cross-dyad/cross-human contest -> compose into
      form (FO dispose).
    summit: S2
    status: BLOCKED
    dependencies:
    - causal_attribution
    note: 'Survivor: ''update the-dyad-practice/README.md?'' -> NO, not yet. Form README has zero defects.'
  falsification_promotable:
    title: Falsification contract -> promotable (v4 provisional)
    goal: Needs a CROSS-HUMAN responder; all 9 dyads share pltrinh1122. External/adoption-gated, not ours
      to manufacture.
    summit: S1
    status: IN_REVIEW
    dependencies: []
  falsify_ci:
    title: falsify.py / validate_falsification CI on falsification/ PRs
    goal: Founding gate (Commons enforcement; the reduce-FO-burden payoff).
    summit: S1
    status: BLOCKED
    dependencies: []
  summit_ratified:
    title: Steward summit S3 ratified + broadcast
    goal: 'AGENT.md Telos + directory #56 + README, all consistent. RATIFIED 2026-06-06 (FO gavel).'
    summit: S3
    status: DONE
    dependencies: []
  dm_locator_remedy:
    title: 'onboard.py teaches the dm_locator remedy (Commons #53)'
    goal: 'Enablement: surface the PUBLIC-mailbox remedy for private-anchor dyads.'
    summit: S1
    status: DONE
    dependencies: []
  directory_index_visible:
    title: 'Directory participant index 1->9 visible (Commons #52)'
    goal: regen_directory_index.py (deterministic, --check) + regenerate. Regenerable-view-not-sovereign-edit.
    summit: S1
    status: DONE
    dependencies: []
  falsification_channel:
    title: Inter-dyad falsification channel LIVE (provisional)
    goal: Contract v1->v4 via cross-model panel; installed in Commons.
    summit: S1
    status: DONE
    dependencies: []
  preflight_built:
    title: bin/preflight.py + bin/state.py
    goal: Tier-1 repo-invariants checker + live-state emitter. The mechanical half of validation.
    summit: b1
    status: DONE
    dependencies: []
  automode_unblock_apply:
    title: Operator applies the autoMode.allow exception so direct push to main stops being classifier
      soft-denied
    goal: Off-machine durability is autonomous as workflow, but the auto-mode classifier soft-denies default-branch
      push ABOVE the settings allowlist. Unblock = an autoMode.allow exception, USER settings only (~/.claude/settings.json).
      Restores autonomous push.
    summit: b1
    type: EXECUTE
    status: DONE
    dependencies: []
    note: 'Gated on the OPERATOR (Covalent gate, NOT a steward build): run `python3 sandbox/apply_automode_allow.py`
      (defaults to USER settings) then verify `claude auto-mode config`. I must not run it (Auto-Mode-Bypass)
      nor self-edit settings (Self-Modification) — I authored the script, the Operator disposes. Durable
      stand-up-surfaced home for gap (b) from the 2026-06-07 read. git-remote-ops-additive-vs-destructive.'
  probe_commons_repo_audit:
    title: 'PROBE: should we have a mechanical/deterministic commons-repo resting-state audit?'
    goal: 'Confirm-or-falsify (discovery) whether a deterministic full-repo Commons audit is defect-earned
      and where it homes. Full riff captured: dialectic/commons-repo-audit-riff.md.'
    summit: S1
    type: PROBE
    status: DONE
    dependencies: []
    invariant: A deterministic Commons resting-state audit is defect-earned (a lived drift case that per-PR
      CI + preflight did NOT catch) and homes in the Commons (steward proposes; invokes the Commons validators,
      never forks them).
    note: 'RACKED (Operator rack: 2026-06-07) for discovery TOMORROW — do not act until un-racked. Parked
      mid-riff; surfaced lean (held loosely): thin, defect-earned, Commons-homed, resting-state audit
      YES; comprehensive steward-side correctness spec NO. Open Qs + 3 cautions in the riff doc. BLOCKED
      encodes racked (frontier has no RACKED status — 2nd time wanted; see riff doc adjacent-noticing).'
